/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.shiksha.com', 'c7.alamy.com', "images.unsplash.com","images.pexels.com"],
    },
};

export default nextConfig;
